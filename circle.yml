machine:
  environment:

dependencies:
  cache_directories:
    - ~/.anyenv
    - local
  pre:
    - bash circle-setup.sh
    - carton install --deployment

test:
  pre:
    - carton install --deployment
  override:
    - carton exec -- prove
