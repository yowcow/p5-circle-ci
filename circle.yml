machine:
  services:
    - mysql
  environment:

dependencies:
  pre:
    - sudo apt-get install -yqq ruby2.0
    - git submodule update --init
    - cd submodules/my-itamae && make && make circle

database:
  override:
    - mysql -u ubuntu circle_test < etc/schema.sql

test:
  pre:
    - make
  override:
    - make test
