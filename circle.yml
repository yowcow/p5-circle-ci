machine:
  services:
    - mysql
  environment:

dependencies:
  cache_directories:
    - ~/.anyenv
    - local
  pre:
    - bash circle-setup.sh
    - make

database:
  override:
    - mysql -u ubuntu circle_test < etc/schema.sql

test:
  pre:
    - make
  override:
    - make test
