machine:
  environment:

dependencies:
  cache_directories:
    - "~/.anyenv"
  #pre:
  #  - bash circle-setup.sh
  post:
    - rm -r ~/.gradle

test:
  #pre:
  #  - carton install --deployment
  #override:
  #  - carton exec -- prove
  override:
    - prove
